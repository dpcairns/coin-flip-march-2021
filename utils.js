export function getActualHeadsOrTails(someNumber) {
    // if (someNumber >= .5) return 'tails';

    // return 'heads';

    // const something = conditional ? 'if true it is this' : 'if false, it is this';
    return someNumber >= .5 ? 'tails' : 'heads';
}

// export const getActualHeadsOrTails = x => x >= .5 ? 'tails' : 'heads';